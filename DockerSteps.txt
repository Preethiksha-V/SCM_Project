👉 server.js

const express = require('express');
const app = express();

app.get('/', (req, res) => {
    res.send("My App Running");
});

app.listen(3000, () => {
    console.log("Server running on port 3000");
});

👉 package.json

{
  "name": "my-app",
  "version": "1.0.0",
  "main": "server.js",
  "dependencies": {
    "express": "^4.18.2"
  }
}

✅ 2. DOCKERFILE (VERY IMPORTANT 🔥)
FROM node:18

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 3000

CMD ["node", "server.js"]
✅ 3. KUBERNETES FILES

👉 deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: my-app
        image: my-app:v1
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000

👉 service.yaml

apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: NodePort
  selector:
    app: my-app
  ports:
    - port: 3000
      targetPort: 3000
      nodePort: 30010

      
🚀 STEP-BY-STEP WORKFLOW (EXAM READY)
🔹 STEP 1: VS CODE
npm install
node server.js

👉 Check: http://localhost:3000

👉 Add .gitignore to avoid pushing node_modules

Create file:

.gitignore

Add:

node_modules/

🔹 STEP 2: GITHUB
git init
git add .
git commit -m "initial"
git remote add origin <your repo>
git push -u origin main

🔹 STEP 3: DOCKER
docker build -t my-app:v1 .
docker run -p 3000:3000 my-app:v1

🔹 STEP 4: DOCKER HUB
docker tag my-app:v1 <username>/my-app:v1
docker push <username>/my-app:v1

🔹 STEP 5: JENKINS PIPELINE (IMPORTANT)
FULL CORRECT PIPELINE
pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                git branch: 'main', url: 'https://github.com/Preethiksha-V/my-app.git'
            }
        }

        stage('Build Docker') {
            steps {
                bat 'docker build -t my-app:v1 .'
            }
        }

        stage('Push DockerHub') {
            steps {
                bat 'docker push preethiksha/my-app:v1'
            }
        }
    }
}

STEP 6: KUBERNETES
kubectl apply -f deployment.yaml
kubectl apply -f service.yaml

🔹 STEP 7: CHECK
kubectl get pods
kubectl get services

🔹 STEP 8: RUN
kubectl port-forward service/my-app-service 3000:3000

👉 Open:

http://localhost:3000
